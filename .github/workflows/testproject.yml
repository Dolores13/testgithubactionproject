name: Stats

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write
    steps:
      - name: Ceckout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with: 
          python-version: '3.13'

      - name: Run script
      - run: python ./testgithubactionproject/testproject.py

      - name: commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit-message: Update testproject
          commit_user_name: Brais Moure [GitHub Actions]
          commit_user_mail: L00196725@atu.ie
          commit_author: L00196725
          
